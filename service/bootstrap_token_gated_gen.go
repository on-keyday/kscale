// Code generated by kscale/access/generator. DO NOT EDIT.
package service

import (
	"context"
	"fmt"

	"github.com/on-keyday/kscale/access"
	"github.com/on-keyday/kscale/access/audit"
	"github.com/on-keyday/kscale/peer"
	pb "github.com/on-keyday/kscale/protobuf/proto"
	pbaccess "github.com/on-keyday/kscale/protobuf/proto/access"
)

// BootstrapTokenGated is the generated ABAC gate for BootstrapTokenService. It implements the full
// pb.BootstrapTokenServiceServer surface as a decorator over Inner (the hand-written
// business): every method authorizes the caller against the resource policy
// (caller from the peer, $env.args from the request, $env.authority from the
// root) and then delegates to Inner. Registering this — never Inner directly —
// is what keeps the gate on every action.
type BootstrapTokenGated struct {
	Inner      pb.BootstrapTokenServiceServer
	Controller access.AccessController
	Resource   access.Resource
	Root       access.RootAuthority // supplies $env.authority
	Audit      audit.Audit
}

var _ pb.BootstrapTokenServiceServer = (*BootstrapTokenGated)(nil)

// authorize runs the ABAC gate for action using the caller carried by the
// peer and args exposed as $env.args.
func (s *BootstrapTokenGated) authorize(ctx context.Context, action string, args []access.Attribute) error {
	p := peer.GetPeer(ctx)
	if p == nil || p.Policy() == nil || p.Policy().User == nil {
		return fmt.Errorf("permission denied: unauthenticated caller")
	}
	envAttrs := []access.Attribute{access.NewAuthorityAttributeMapper(s.Root)}
	if len(args) > 0 {
		envAttrs = append(envAttrs, access.NewAttributeManager("args", args))
	}
	policyCtx := p.Policy().
		CollectResource(s.Resource).
		CollectEnvironment(access.NewEnvironment(envAttrs)).
		CollectAction(access.NewAction(action, nil)).
		Build(ctx, s.Audit)
	if err := s.Controller.CanAccess(policyCtx); err != nil {
		return fmt.Errorf("permission denied: %w", err)
	}
	return nil
}

func (s *BootstrapTokenGated) Issue(ctx context.Context, req *pbaccess.ResourceBootstrapTokenActionIssueArgsDTO) (*pbaccess.ResourceBootstrapTokenActionIssueResponseDTO, error) {
	if err := s.authorize(ctx, "issue", []access.Attribute{access.NewAttribute("duration", req.Duration), access.NewAttribute("appName", req.AppName), access.NewAttribute("domain", req.Domain), access.NewAttribute("expires_period", req.ExpiresPeriod)}); err != nil {
		return nil, err
	}
	return s.Inner.Issue(ctx, req)
}

func (s *BootstrapTokenGated) Revoke(ctx context.Context, req *pbaccess.ResourceBootstrapTokenActionRevokeArgsDTO) (*pbaccess.ResourceBootstrapTokenActionRevokeResponseDTO, error) {
	if err := s.authorize(ctx, "revoke", []access.Attribute{access.NewAttribute("token", req.Token)}); err != nil {
		return nil, err
	}
	return s.Inner.Revoke(ctx, req)
}
